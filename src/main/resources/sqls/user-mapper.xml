<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.restapi.Restfull.API.Server.interfaces.mappers.UserMapper">
    <insert id="insertUser">
        insert into user(name, email, sns, reg_date, is_artist, profile_img, token)
        VALUES(#{name}, #{email}, #{sns}, #{reg_date}, #{is_artist}, #{profile_img}, #{token})
    </insert>
    <select id="selectUserByEmail" resultType="User">
        select * from user where email=#{email} and sns=#{sns}
    </select>
</mapper>